
// Example supabase-config (sanitized):
// Copy this file to `assets/js/supabase-config.js` and fill with your project values.
// IMPORTANT: Never commit real keys to public repositories.

window.SUPABASE = {
  url: "https://your-project.supabase.co",
  anonKey: "your-anon-key-here",
  bucket: "banners"
};

// Note: This example contains placeholders. Replace with your own values and
// keep the real `supabase-config.js` out of version control if it contains secrets.
