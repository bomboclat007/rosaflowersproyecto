Place your footer logo image here as `logopequeno.jpg` (no accented filename recommended).

Why:
- The project injects `assets/js/footer-logo-replace.js` which updates footer <img> tags to point to `assets/img/logopequeno.jpg`.

Instructions:
1. Copy your JPG to `assets/img/logopequeno.jpg`.
2. If you prefer PNG, name it `logopequeno.png` and update the script path accordingly.

Note: I did not add a binary image file. Please upload your actual `logopequeno.jpg` to this folder so the footer shows the correct image site-wide.